<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.kuleuven.testgeography" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestGeography" >
        <activity
            android:name=".activities.QuizOpenActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ChangeUsernameActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.FinishQuizActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PopRegionActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PopTypeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UserPageActivity"
            android:exported="false" />
        <activity
            android:name=".activities.QuizMCActivity"
            android:exported="false" />
        <activity android:name=".activities.MainActivity" >

            <!-- android:exported="true" > -->
            <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".activities.LoginRegisterActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>